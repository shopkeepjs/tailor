#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# npm run lint

BRANCH="$(git rev-parse --abbrev-ref HEAD)"
if [[ $BRANCH = development ]]; then
  echo 'ABORT';
  exit 1;
fi

echo $BRANCH;
echo 'SHOULD NOT SEE';

# npm test
# npm run package
